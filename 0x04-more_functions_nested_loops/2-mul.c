#include "main.h"

/**
 * mul - Returns multiple of 2 integers
 * @a: One of the integers of the expression
 * @b: The other integer in the expression
 *
 * Return: Product
 */
int mul(int a, int b)
{
	return (a * b);
}
